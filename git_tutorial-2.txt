git basic commands

git add [file_name] # to add the files in staging
git commit # to stage to files
git rm <file_name> # to remove the staged the file
git push/pull # to push/pull the files from/to git remote branches
